import {auth} from"../lib/auth"
import prisma from "@/db/lib/prisma"
import Link from "next/link"
import { redirect } from "next/navigation"

export default async function Dashboard() {
  // 1. Auth Check
  
  // 2. DB Query (Paste your logic here)

  // 3. JSX Return
}